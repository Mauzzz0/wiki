/**
 * Default locale
 */
const locale = 'ru'

/**
 * PageID for template for new Student
 */
const templatePageId = 6

/**
 * PageID for Plan (Program) Page
 */
const planPageId = 2

/**
 * PageID for Content Page
 */
const contentPageId = 232

/**
 * GroupID for "Allow Public" common permissions for every Student
 */
const allowPublicGroupId = 4

module.exports = {
  locale,
  templatePageId,
  planPageId,
  contentPageId,
  allowPublicGroupId
}
